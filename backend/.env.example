# DATABASE_URL format for docker-compose service 'db'
DATABASE_URL="postgresql://atlas:atlas_password@db:5432/atlasdb?schema=public"
JWT_SECRET="mude_para_uma_chave_super_segura"
PORT=4000

# Optional: file upload path (inside container)
UPLOADS_DIR="./uploads"
